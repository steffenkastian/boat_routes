import 'package:flutter/material.dart';

class RegattaScreen extends StatelessWidget {
  const RegattaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Regatta fahren')),
      body: const Center(child: Text('Diese Seite wird später ergänzt.')),
    );
  }
}
